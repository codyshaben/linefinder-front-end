import React from 'react';
import './MapView.scss';

function MapView() {
    return (
        <div className='map-listing'>
            <p>hello from the map page</p>
        </div>
    );
};

export default MapView;